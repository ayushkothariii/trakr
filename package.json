{
  "name": "trackr",
  "version": "1.0.0",
  "description": "Self-hosted deep-link + install attribution platform. Track app installs from organic posts across platforms with custom aliases.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "@libsql/client": "^0.14.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
